server:
  port: ${PORT:8888}
  
logging:
  file: logs/config-server.log
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'DEBUG'

spring:
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/fburg/workspace/Fastest-config